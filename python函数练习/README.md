# python函数练习

文件说明：
- function_exercises.py：PDF 中的函数练习题答案

运行方式：
在 PyCharm 中打开本文件夹，右键运行 function_exercises.py。

包含功能：
- 统计字符串中数字、字母、空格、其他字符数量
- 判断对象长度是否大于5
- 检查字符串、列表、元组是否有空内容
- 列表长度大于2时只保留前两个元素
- 获取列表或元组中奇数索引对应的元素
